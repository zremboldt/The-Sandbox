<h1>☑ Tasks</h1>

<div>
  <h4>All Categories</h4>
  <ul>
    <% @categories.each do |category| %>
      <li><%= link_to(category.name, category_path(category)) %></li>
    <% end %>
  </ul>
</div>

<%= link_to("Create new category", new_category_path) %>
<%= link_to("View all tasks", tasks_path(page: 1)) %>